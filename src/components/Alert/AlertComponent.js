import React from "react";
import { Alert } from 'react-bootstrap';

const AlertComponent = (props) => {
    return (
        <div>
            <Alert variant='succes'>Test Alert</Alert>
        </div>
    )
}